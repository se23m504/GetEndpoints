name: GetEndpoints

services:
  api:
    build:
      context: ../src
      dockerfile: ../docker/Dockerfile
    ports:
      - 5000:5000
    environment:
      FLASK_ENV: production
    volumes:
      - ../src/instance:/app/instance
